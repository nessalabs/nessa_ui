# Kiro CLI fixtures

Synthetic JSONL for the agent-stream Storybook story and unit tests.

<!-- TODO(kiro-live): Replace these with live captures from:
  kiro-cli chat --no-interactive --trust-all-tools --output-format stream-json "<prompt>"
Also add an ACP capture via `kiro-cli acp` once credits/access return, then
update KIRO_CHAT_PROVENANCE / KIRO_ACP version + capturedOn in the same commit.
-->
